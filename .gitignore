# Build output
build/
*.o
*.com
*.com.dbg
*.aarch64.elf

# Vendored gems (installed per-project)
vendor/
!vendor/cjson/
.bundle/

# Managed Ruby tooling
.ruby-lsp/

# AI agent working dirs
.claude/
.kimi/
.agent-relay-ftw-agents
tmp/

# Corpus test data (snarfed from RubyGems/GitHub)
snarf/

# Corpus pipeline (scripts protected via vendor/ruby-binary/; data excluded)
lode/
lode-backup/

# Scratch scripts
gem_matrix.sh
sanity_check.sh
update_agent.sh
uv_docker.sh
hello.rb

# Existing Ruby project files (wow will generate its own)
Gemfile
Gemfile.lock
.ruby-version
Wow

# There's a repo-level .gitignore at the root. No non-public exclude file beyond the default empty .git/info/exclude.

# Generated by ./configure
config.mk

# Generated test certs (regenerate with tests/certs/generate.sh)
tests/certs/*.pem

# wowx comparison harness outputs (generated by run_comparison.sh / fetch_versions.sh)
tests/wowx/results/
tests/wowx/version_matrix.tsv

# Stray dependency files from test builds
a-*.d

# Editor / OS
*~
*.swp
*.swo
.DS_Store
